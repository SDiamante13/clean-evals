# Story 7.1 — Create `setupAgentEval()` for vitest.setup.ts

**Epic:** Developer Experience

## Description

Single function that registers all custom matchers and configures a default provider for zero-config setup.

## Tasks

- Implement `setupAgentEval(options?)` function
- Registers all custom matchers with Vitest
- Accepts optional default judge provider config

## Acceptance Criteria

- Example setup file works with zero additional config
