# Story 1.1 — Initialize TypeScript Library Scaffolding

**Epic:** Project Foundation

## Description

Set up the base TypeScript library project with build and test tooling.

## Tasks

- Configure `package.json` (name: `agent-eval`, ESM)
- Configure `tsconfig.json` with strict mode and declaration files
- Configure Vitest
- Create entry point `src/index.ts` exporting nothing yet

## Acceptance Criteria

- `npm run build` passes with zero errors
- `npm test` passes with zero tests
