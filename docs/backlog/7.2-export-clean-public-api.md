# Story 7.2 — Export Clean Public API from `src/index.ts`

**Epic:** Developer Experience

## Description

Finalize the public API surface with clean named exports and TypeScript declaration augmentation for Vitest's `expect`.

## Tasks

- Named exports: matchers, providers, utilities, types
- TypeScript declaration augmentation for Vitest `expect`

## Acceptance Criteria

- Consumer project can `import { setupAgentEval, OpenAIJudgeProvider } from 'agent-eval'`
- TypeScript autocomplete works for custom matchers
