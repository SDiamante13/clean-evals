# Ralph Progress — agent-eval
# Branch: ralph/agent-eval-library
# Started: 2026-01-30

## Codebase Patterns
- Use `createTestTracer()` for isolated per-test OpenTelemetry spans
- Export types separately with `export type` from index.ts
- Span attribute keys: generic (`tool.name`, `tool.arguments`), OpenAI (`gen_ai.request.function_call.*`), LLM (`llm.function_call.*`)
- Run quality checks: `./node_modules/.bin/tsc --noEmit` and `./node_modules/.bin/vitest run`
- Must `npm install` first — node_modules not committed
- Ralph files are in `scripts/ralph/`, not `ralph/`
- Custom Vitest matchers: implementation in `src/matchers/`, registered in `register.ts`, types in `types.ts`
- Type augmentation for custom matchers uses `declare module 'vitest'` with `Assertion<T>` and `AsymmetricMatchersContaining`

---

## 2026-01-30 - US-001 & US-002
- Already implemented in prior commits
- Marked as passing
---

## 2026-01-30 - US-003
- Implemented `findToolCalls(spans)` in `src/find-tool-calls.ts`
- Extracts tool name and args from spans using multiple attribute key conventions
- Files changed: `src/find-tool-calls.ts`, `src/find-tool-calls.test.ts`, `src/index.ts`, `scripts/ralph/prd.json`
- **Learnings for future iterations:**
  - OpenTelemetry spans use `ReadableSpan` from `@opentelemetry/sdk-trace-base`
  - Attribute values can be string, number, boolean, or arrays — always parse JSON strings for args
---

## 2026-01-31 - US-004
- Implemented `evaluateTrace({ spans, expected })` in `src/evaluate-trace.ts`
- Compares extracted tool calls against expected calls with exact name match and deep-equal args
- Score = matched / expected (0.0-1.0), pass when score === 1.0
- Files changed: `src/evaluate-trace.ts`, `src/evaluate-trace.test.ts`, `src/index.ts`
- **Learnings for future iterations:**
  - `evaluateTrace` uses `findToolCalls` internally — no need to call it separately
  - Args comparison uses sorted JSON.stringify for deep equality
  - When expected has no args, any args on the actual call are accepted
---

## 2026-01-31 - US-005
- Already implemented in prior commits (cab5ae5, bd22bf0)
- `toPassTraceEval()` custom matcher in `src/matchers/to-pass-trace-eval.ts`
- Registration in `src/matchers/register.ts`, type augmentation in `src/matchers/types.ts`
- Verified: lint, typecheck, and all 21 tests pass
- Marked PRD as `passes: true`
- **Learnings for future iterations:**
  - Custom matchers registered via `expect.extend()` in a separate register.ts file
  - Type augmentation for Vitest matchers goes in `src/matchers/types.ts` using `declare module 'vitest'`
  - `buildToolSpan()` from test-helpers used for all matcher tests
---
## 2026-01-31 - US-006
- Implemented JudgeProvider interface and OpenAIJudgeProvider
- System prompt instructs LLM to return JSON { score, reasoning }
- Integration test with mocked HTTP confirms correct prompt construction and response parsing
- Files changed: src/judges/*.ts (interface, provider, system-prompt, parse-judge-response, index), src/index.ts
- **Learnings for future iterations:**
  - JudgeProvider interface defined in judge-provider.ts with judge(prompt): Promise<JudgeResult>
  - parseJudgeResponse() extracts JSON from surrounding text, validates score and reasoning fields
  - All judge providers share the same JUDGE_SYSTEM_PROMPT from system-prompt.ts
---

## 2026-01-31 - US-007
- AnthropicJudgeProvider already implemented in prior commit (e6d84a6)
- Verified: lint, typecheck, and all 32 tests pass
- Marked PRD as `passes: true`
- Files: src/judges/anthropic-judge-provider.ts, src/judges/anthropic-judge-provider.test.ts, src/judges/index.ts
- **Learnings for future iterations:**
  - Anthropic SDK uses `messages.create()` with separate `system` param (not in messages array like OpenAI)
  - Response content is an array of ContentBlock objects; check `block.type === 'text'`
  - Mock pattern: mock `@anthropic-ai/sdk` default export as a class with `messages = { create: mockFn }`
---

## 2026-01-31 - US-008
- OllamaJudgeProvider already implemented in prior commit (6a07ea7)
- Uses native `fetch` to call Ollama's `/api/chat` endpoint
- Verified: lint, typecheck, and all 37 tests pass
- Marked PRD as `passes: true`
- Files: src/judges/ollama-judge-provider.ts, src/judges/ollama-judge-provider.test.ts, src/judges/index.ts
- **Learnings for future iterations:**
  - Ollama uses native fetch (no SDK needed) — mock with `vi.stubGlobal('fetch', mockFn)`
  - Ollama chat API response shape: `{ message: { content: string } }`
  - Default endpoint: `http://localhost:11434/api/chat`, default model: `llama3`
---

## 2026-01-31 - US-009
- Implemented `evaluateOutput({ output, rubric, judge })` in `src/evaluate-output.ts`
- Constructs grading prompt with rubric and output, calls judge, returns pass/fail based on threshold
- Default passThreshold is 0.7; can be overridden via options
- Unit tests with fake judge verify pass/fail/warn thresholds and boundary conditions
- Files changed: src/evaluate-output.ts, src/evaluate-output.test.ts, src/index.ts
- Verified: lint, typecheck, and all 43 tests pass
- **Learnings for future iterations:**
  - Use builder functions for test judges (e.g., `buildFakeJudge(result)`)
  - evaluateOutput accepts `passThreshold` and `warnThreshold` as optional params
  - Grading prompt format: `## Rubric\n{rubric}\n\n## Agent Output\n{output}`
---

## 2026-01-31 - US-010
- Implemented `toPassEval()` Vitest custom matcher in `src/matchers/to-pass-eval.ts`
- Matcher accepts `{ rubric, judge, passThreshold?, warnThreshold? }` options
- Calls `evaluateOutput()` internally and returns matcher result with pass/fail and reasoning
- Reasoning is included in both success and failure messages
- Unit tests with fake judge verify pass/fail thresholds and reasoning display
- Files changed: src/matchers/to-pass-eval.ts, src/matchers/to-pass-eval.test.ts, src/matchers/register.ts, src/matchers/types.ts, src/index.ts
- Verified: lint, typecheck, and all 49 tests pass
- **Learnings for future iterations:**
  - Custom matchers for async operations return `Promise<MatcherResult>` and type signature uses `Promise<T>`
  - Matcher reasoning should be visible in both pass and fail message branches
  - Register all matchers in `src/matchers/register.ts` using `expect.extend({})`
---

## 2026-01-31 - US-011
- Already implemented in prior commit (cbeaba1)
- `distillTrace(spans)` in `src/distill-trace.ts` converts spans to chronological narrative
- Verified: lint, typecheck, and all 55 tests pass
- Marked PRD as `passes: true`
- **Learnings for future iterations:**
  - `distillTrace` filters for tool spans, sorts by start time, formats as numbered steps
  - `buildToolSpan()` supports `result` and `startTimeMs` options for trace tests
---

## 2026-01-31 - US-012
- Extended `toPassTraceEval` with optional `{ judge, rubric }` for LLM grading
- When judge+rubric provided, distills trace and sends to judge with rubric prompt
- Matcher is now async (returns `Promise<MatcherResult>`)
- Added 4 new tests: prompt contains distilled story, fail on low score, reasoning in failure, skip judge without rubric
- Files changed: src/matchers/to-pass-trace-eval.ts, src/matchers/to-pass-trace-eval.test.ts, src/matchers/types.ts
- Verified: lint, typecheck, and all 59 tests pass
- **Learnings for future iterations:**
  - Converting sync matcher to async: change return type to `Promise<MatcherResult>` and update type declaration
  - Judge prompt format for traces: `## Rubric\n{rubric}\n\n## Agent Trace\n{story}`
  - Tests for async matchers use `await expect(...).rejects.toThrow()` pattern
---

## 2026-01-31 - US-013
- Already implemented in prior commit (6643745)
- `evaluateHybrid()` in `src/evaluate-hybrid.ts` runs deterministic check first, skips LLM if it fails
- Verified: lint, typecheck, and all 63 tests pass
- Marked PRD as `passes: true`
- **Learnings for future iterations:**
  - `evaluateHybrid` composes `evaluateTrace` + `evaluateOutput` with fail-fast semantics
  - Result includes `skippedLlm` boolean and optional `llmResult`
  - Test pattern: verify `judge.judge` was `not.toHaveBeenCalled()` when deterministic fails
---

## 2026-01-31 - US-014
- Already implemented in prior commit (08b6a2c)
- `toPassHybridEval()` matcher in `src/matchers/to-pass-hybrid-eval.ts`
- Registered in register.ts, types in types.ts, exported from index.ts
- Verified: lint, typecheck, and all 68 tests pass
- Marked PRD as `passes: true`
- **Learnings for future iterations:**
  - `toPassHybridEval` composes `evaluateHybrid` internally
  - Fail-fast: when deterministic check fails, message shows "LLM judge was skipped"
  - Builder function `buildFakeJudge(result)` used for all judge test doubles
---

## 2026-01-31 - US-015
- Already implemented in prior commit (2517636)
- `runWithSamples()` in `src/run-with-samples.ts` runs eval function k times, reports pass@k
- `SamplesConfig` and `SampledResult` types exported, `formatSampledMessage` for matcher output
- All three matchers (`toPassEval`, `toPassTraceEval`, `toPassHybridEval`) support `samples` option
- Verified: lint, typecheck, and all 77 tests pass
- Marked PRD as `passes: true`
- **Learnings for future iterations:**
  - `runWithSamples` takes `evalFn: () => Promise<{pass, score}>` and `SamplesConfig { count }`
  - pass@k semantics: passes if >= 1 sample passes
  - Reports aggregate stats: min, max, mean, passCount
---
